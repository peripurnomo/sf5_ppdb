<div class="row">
    <div class="col s12">
        {% for messages in app.flashes(['Success', 'Warning']) %}
            {% for message in messages %}
                <div>
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
    </div>
</div>